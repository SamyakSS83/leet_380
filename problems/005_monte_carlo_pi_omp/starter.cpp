#include <vector>
#include <cstdlib>
#include <omp.h>

// Estimate pi using Monte Carlo with n samples and given seed.
// Use per-thread RNG seeds to avoid race conditions.
double monte_carlo_pi(long long n, unsigned seed) {
    // TODO: implement
    return 0.0;
}
