#include <vector>
#include <algorithm>
#include <omp.h>

// Sort v in-place using parallel quicksort with OMP tasks.
// Use std::sort for subarrays smaller than cutoff.
void parallel_quicksort(std::vector<int>& v, int cutoff) {
    // TODO: implement
}
