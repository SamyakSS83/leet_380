#include <mpi.h>
#include <vector>

// Ping-pong between rank 0 and rank 1 for N rounds with msg_size doubles.
// Return average round-trip time in microseconds (rank 0 measures, others return 0).
double mpi_ping_pong(int N, int msg_size) {
    // TODO: implement
    return 0.0;
}
