#include <vector>
#include <omp.h>

// Compute the sum of all elements in v using OpenMP parallelism.
// Use a parallel reduction.
double parallel_sum(const std::vector<double>& v) {
    // TODO: implement
    return 0.0;
}
