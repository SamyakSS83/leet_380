#include <vector>
#include <cmath>
#include <omp.h>

// Compute sum of base^work_sizes[i] for all i, using schedule(dynamic).
double irregular_sum(const std::vector<int>& work_sizes, double base) {
    // TODO: implement using schedule(dynamic)
    return 0.0;
}
