#include <vector>
#include <string>
#include <utility>
#include <omp.h>

// Count word frequencies in parallel. Return sorted (word, count) pairs.
std::vector<std::pair<std::string,int>> parallel_word_count(
    const std::vector<std::string>& words) {
    // TODO: implement
    return {};
}
