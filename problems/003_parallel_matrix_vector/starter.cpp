#include <vector>
#include <omp.h>

// Compute y = A * x using OpenMP parallelism (parallelize over rows).
std::vector<double> parallel_matvec(
    const std::vector<std::vector<double>>& A,
    const std::vector<double>& x) {
    // TODO: implement
    return {};
}
