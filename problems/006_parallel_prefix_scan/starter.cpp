#include <vector>
#include <omp.h>

// Compute inclusive prefix sum of v in parallel.
// out[i] = v[0] + v[1] + ... + v[i]
std::vector<double> parallel_prefix_scan(const std::vector<double>& v) {
    // TODO: implement
    return {};
}
