#include <vector>
#include <omp.h>

// Sort vector of unsigned ints using parallel LSD radix sort (8-bit digits, 4 passes).
std::vector<unsigned> parallel_radix_sort(std::vector<unsigned> v) {
    // TODO: implement
    return v;
}
