#include <vector>
#include <omp.h>

// Compute sum of 2D matrix using nested parallel regions.
double nested_parallel_sum(const std::vector<std::vector<double>>& mat) {
    // TODO: use omp_set_nested(1) and nested parallel regions
    return 0.0;
}
