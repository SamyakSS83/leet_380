#include <vector>
#include <omp.h>

// Compute y[i] = a * x[i] + y[i] for all i, in-place, using OpenMP.
void parallel_saxpy(double a, const std::vector<double>& x, std::vector<double>& y) {
    // TODO: implement
}
