#include <vector>
#include <omp.h>

// Find the maximum subarray sum using parallel divide-and-conquer.
// Return 0 if array is empty or all negative (empty subarray convention).
double max_subarray_sum(const std::vector<double>& v) {
    // TODO: implement
    return 0.0;
}
